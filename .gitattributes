# Images are stored as regular git objects on purpose: the repo's binary
# assets exceed GitHub's free Git LFS quota (1 GB), so LFS checkout fails in
# CI. Images are needed at build time, so they must be plain git blobs.
*.pdf filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
*.pptx filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
